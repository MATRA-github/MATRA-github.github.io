var attempt = 3; // Variable to count number of attempts.
// Below function Executes on click of login button.
function validate(){
var username = document.getElementById("username").value;
var password = document.getElementById("password").value;
if ( username == "CR234163" && password == "070810110345"){
alert ("Log masuk berjaya!");
window.location = "pelaporanharithanaqi.html"; // Redirecting to other page.
return false;
}
else{
attempt --;// Decrementing by one.
// Disabling fields after 3 attempts.
if( attempt == 0){
alert("Log masuk gagal! Sila refresh untuk mencuba lagi!")
document.getElementById("username").disabled = true;
document.getElementById("password").disabled = true;
document.getElementById("submit").disabled = true;

return false;
}
}

if ( username == "CR234268" && password == "070613160036"){ //sarah raihanah
    alert ("Log masuk berjaya!");
    window.location = "pelaporansarahraihana.html"; // Redirecting to other page.
    return false;
    }
    else{
    attempt --;// Decrementing by one.
    // Disabling fields after 3 attempts.
    if( attempt == 0){
    alert("Log masuk gagal! Sila refresh untuk mencuba lagi!")
    document.getElementById("username").disabled = true;
    document.getElementById("password").disabled = true;
    document.getElementById("submit").disabled = true;
    }
}

if ( username == "CR234142" && password == "070222140118"){   //fatin nur aina
    alert ("Log masuk berjaya!");
    window.location = "pelaporanfatinnuraina.html"; // Redirecting to other page.
    return false;
    }
    else{
    attempt --;// Decrementing by one.

    // Disabling fields after 3 attempts.
    if( attempt == 0){
    alert("Log masuk gagal! Sila refresh untuk mencuba lagi!")
    document.getElementById("username").disabled = true;
    document.getElementById("password").disabled = true;
    document.getElementById("submit").disabled = true;
    }
}


if ( username == "CR234209" && password == "070810860015"){   //afkam
    alert ("Log masuk berjaya!");
    window.location = "pelaporanafham.html"; // Redirecting to other page.
    return false;
    }
    else{
    attempt --;// Decrementing by one.

    // Disabling fields after 3 attempts.
    if( attempt == 0){
    alert("Log masuk gagal! Sila refresh untuk mencuba lagi!")
    document.getElementById("username").disabled = true;
    document.getElementById("password").disabled = true;
    document.getElementById("submit").disabled = true;
    }
}

if ( username == "CR234197" && password == "070623060100"){   //ain syaza
    alert ("Log masuk berjaya!");
    window.location = "pelaporanainsyaza.html"; // Redirecting to other page.
    return false;
    }
    else{
    attempt --;// Decrementing by one.

    // Disabling fields after 3 attempts.
    if( attempt == 0){
    alert("Log masuk gagal! Sila refresh untuk mencuba lagi!")
    document.getElementById("username").disabled = true;
    document.getElementById("password").disabled = true;
    document.getElementById("submit").disabled = true;
    }
}


if ( username == "CR234163" && password == "071026140043"){   //zafran
    alert ("Log masuk berjaya!");
    window.location = "pelaporanzafran.html"; // Redirecting to other page.
    return false;
    }
    else{
    attempt --;// Decrementing by one.

    // Disabling fields after 3 attempts.
    if( attempt == 0){
    alert("Log masuk gagal! Sila refresh untuk mencuba lagi!")
    document.getElementById("username").disabled = true;
    document.getElementById("password").disabled = true;
    document.getElementById("submit").disabled = true;
    }
}



if ( username == "CR234097" && password == "071004100921"){   //badak
    alert ("Log masuk berjaya!");
    window.location = "pelaporanaiman.html"; // Redirecting to other page.
    return false;
    }
    else{
    attempt --;// Decrementing by one.

    // Disabling fields after 3 attempts.
    if( attempt == 0){
    alert("Log masuk gagal! Sila refresh untuk mencuba lagi!")
    document.getElementById("username").disabled = true;
    document.getElementById("password").disabled = true;
    document.getElementById("submit").disabled = true;
    }
}





if ( username == "CR234186" && password == "0701196060574"){   //Ell
    alert ("Log masuk berjaya!");
    window.location = "pelaporanellyana.html"; // Redirecting to other page.
    return false;
    }
    else{
    attempt --;// Decrementing by one.

    // Disabling fields after 3 attempts.
    if( attempt == 0){
    alert("Log masuk gagal! Sila refresh untuk mencuba lagi!")
    document.getElementById("username").disabled = true;
    document.getElementById("password").disabled = true;
    document.getElementById("submit").disabled = true;
    }
}




if ( username == "CR234123" && password == "070630141056"){   //alyssa
    alert ("Log masuk berjaya!");
    window.location = "pelaporanalyssa.html"; // Redirecting to other page.
    return false;
    }
    else{
    attempt --;// Decrementing by one.

    // Disabling fields after 3 attempts.
    if( attempt == 0){
    alert("Log masuk gagal! Sila refresh untuk mencuba lagi!")
    document.getElementById("username").disabled = true;
    document.getElementById("password").disabled = true;
    document.getElementById("submit").disabled = true;
    }
}





if ( username == "CR234111" && password == "071207140202"){   //basyirah
    alert ("Log masuk berjaya!");
    window.location = "pelaporanbasyirah.html"; // Redirecting to other page.
    return false;
    }
    else{
    attempt --;// Decrementing by one.

    // Disabling fields after 3 attempts.
    if( attempt == 0){
    alert("Log masuk gagal! Sila refresh untuk mencuba lagi!")
    document.getElementById("username").disabled = true;
    document.getElementById("password").disabled = true;
    document.getElementById("submit").disabled = true;
    }
}
if ( username == "CR234159" && password == "070211140278"){   //tengku
    alert ("Log masuk berjaya!");
    window.location = "pelaporantengku.html"; // Redirecting to other page.
    return false;
    }
    else{
    attempt --;// Decrementing by one.

    // Disabling fields after 3 attempts.
    if( attempt == 0){
    alert("Log masuk gagal! Sila refresh untuk mencuba lagi!")
    document.getElementById("username").disabled = true;
    document.getElementById("password").disabled = true;
    document.getElementById("submit").disabled = true;
    }
}
}



